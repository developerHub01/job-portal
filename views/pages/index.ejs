<% if (user) { %>
  <%- include('../partials/navbar', {user}); %>
    <% } else { %>
      <%- include('../partials/navbar',); %>
        <% } %>


          <%- include('../partials/hero-slider'); %>

            <section class="w-full max-w-6xl mx-auto px-5 py-8 flex flex-col gap-8">
              <h1 class="text-center text-4xl font-bold ">All Jobs (<%=jobList?.length %>)</h1>
              <div class="w-full grid md:grid-cols-2 gap-8">
                <% jobList.forEach(function(jobs, index){ %>
                  <%- include('../partials/job-card', jobs); %>
                    <% }); %>
              </div>
            </section>